[project]
name = "mixtura-backend"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = []

[tool.uv.workspace]
members = [
    "auth-service",
    "mixer-service",
    "server-service",
    "tournament-service",
    "user-service",
]
